# BEM file structure generator
Simple program to generate BEM file structure.

## How to install
```bash
python -m venv venv
. venv/bin/activate
pip install -r requirements.txt
```

## How to use
Run following command:
```
python genbemfs.py example/
```